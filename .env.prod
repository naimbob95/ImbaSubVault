# Production Environment Configuration

# MongoDB
MONGO_INITDB_ROOT_USERNAME=admin #change this to more secure username
MONGO_INITDB_ROOT_PASSWORD=change-this-secure-password
MONGO_INITDB_DATABASE=imbasubvault #you can change this value

# Database
MONGODB_URI=mongodb://admin:change-this-secure-password@mongodb:27017/imbasubvault?authSource=admin

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=1d

# App
PORT=5001
NODE_ENV=production

# Email Configuration (Production SMTP)
SMTP_HOST=your-smtp-server.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
EMAIL_FROM=noreply@yourdomain.com
FRONTEND_URL=https://yourdomain.com

# Frontend
NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# Mailpit Authentication (Only for development - remove in production)
# Don't use Mailpit for Production, find another online email service for production
